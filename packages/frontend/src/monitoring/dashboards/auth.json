{
  "title": "Authentication Dashboard",
  "panels": [
    {
      "title": "Login Success Rate",
      "type": "graph",
      "query": "sum(rate({app=\"frontend\"} | json | type=\"auth.login.success\" [5m])) / sum(rate({app=\"frontend\"} | json | type=~\"auth.login.*\" [5m]))"
    },
    {
      "title": "Failed Logins by Provider",
      "type": "graph",
      "query": "sum by (provider) (rate({app=\"frontend\"} | json | type=\"auth.login.error\" [5m]))"
    }
  ]
}
